<div class="container">
<p id="notice"><%= notice %></p>
  <h2 class="title text-md font-bold">
    <%= @course.title %>
  </h2>
  <div class="card rounded-md m-2">
    <%= @course.content %>
  </div>


<%= link_to 'Edit', edit_course_path(@course) %> |
<%= link_to 'Back', courses_path %>
</div>