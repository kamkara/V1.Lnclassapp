<%= form_with(model: level) do |form| %>
  <% if level.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(level.errors.count, "error") %> prohibited this level from being saved:</h2>

      <ul>
        <% level.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    
    <%= form.text_field :title , class:"p-2 rounded-md ", placeholder:"title" %>
  </div>


  <div class="actions mt-2">
    <%= form.submit "Create Level", class:"bg-blue-500 p-2 border rounded-md" %>
  </div>
<% end %>
